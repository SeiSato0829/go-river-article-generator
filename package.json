{
  "name": "go-river-article-generator",
  "version": "1.0.0",
  "description": "Go-River 音声自動記事生成システム",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'Static site - no build needed'",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.52.0",
    "node-fetch": "^3.3.2",
    "replicate": "^1.0.1"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
